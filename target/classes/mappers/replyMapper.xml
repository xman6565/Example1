<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="replyMapper">

	<!-- 댓글 조회 -->
	<select id="readReply" resultType="kr.co.vo.ReplyVO">
		select rno,subject, writer, regdate
		  from mp_reply
		 where bno =#{bno}
	</select>
	
	<!-- 댓글 작성 -->
	<insert id="writeReply">
		insert into mp_reply(bno,rno,subject,writer)
		values (#{bno},mp_reply_seq.nextval,#{subject},#{writer})
	</insert>
	
	<!-- 댓글 수정 -->
	<update id="updateReply" parameterType="kr.co.vo.ReplyVO">
		update mp_reply set subject = #{subjcet}
		where rno = #{rno}
	</update>
	
	<!-- 댓글 삭제 -->
	<delete id="deleteReply" parameterType="kr.co.vo.ReplyVO">
		delete from mp_reply
		 where rno = #{rno}
	</delete>
	
	<!-- 선택된 댓글 조회 -->
	<select id="selectReply" resultType="kr.co.vo.ReplyVO">
		select bno,rno,subject,writer,regdate
		  from mp_reply
		 where rno = #{rno}	
	</select>
	
</mapper>